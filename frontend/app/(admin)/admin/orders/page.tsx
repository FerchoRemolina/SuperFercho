import { PlaceholderScreen } from "@/shared/ui/placeholder-screen";

export default function AdminOrdersPage() {
  return (
    <PlaceholderScreen
      title="Pedidos (admin)"
      description="El listado admin usará GET /api/v1/admin/orders. Ventas es el mismo listado con filtro de estado."
    />
  );
}
