import { PlaceholderScreen } from "@/shared/ui/placeholder-screen";

export default function LoginPage() {
  return (
    <PlaceholderScreen
      title="Iniciar sesión"
      description="El formulario de login se conectará a POST /api/v1/auth/login en una fase posterior."
    />
  );
}
