# Browser same-origin calls use /api/v1 via the Next.js rewrite. Leave empty.
NEXT_PUBLIC_API_BASE_URL=

# Server-side origin of Spring Boot for RSC / Node fetch. Never a secret.
SUPERFERCHO_API_ORIGIN=http://localhost:8080
