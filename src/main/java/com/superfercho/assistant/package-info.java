/**
 * Assistant module. Owns conversation orchestration, LLM port, and tool
 * allowlist. Business rules remain in Catalog, Shopping, Orders, Identity,
 * and Knowledge. REST, provider LLM adapters, MCP, and persistence are
 * implemented in later phases.
 */
package com.superfercho.assistant;
