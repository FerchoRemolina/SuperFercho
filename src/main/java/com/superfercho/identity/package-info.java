/**
 * Identity module. Owns users, addresses, roles, registration, authentication,
 * and JWT security wiring. HTTP routes are authorized by role. REST exposes
 * registration, authentication, and address operations.
 */
package com.superfercho.identity;
